- @posts.each do |post|
	%h2= link_to post.title, post
	%p= truncate(post.content, length: 20)
	%p
		Published at
		= time_ago_in_words(post.created_at)
	%br/

= link_to "+ post", new_post_path