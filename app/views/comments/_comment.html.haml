.comment
	%p= comment.comment
	%p= comment.user.email

	-if comment.user == current_user
		= link_to "Edit Reply", edit_post_comment_path(comment.post, comment)
		= link_to "Delete Reply", [comment.post, comment], method: :delete, data: { confirm: "Sure?" }